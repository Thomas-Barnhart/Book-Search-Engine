// Importing type definitions from the './typeDefs' file
const typeDefs = require('./typeDefs');

// Importing resolvers from the './resolvers' file
const resolvers = require('./resolvers');

// Exporting the type definitions and resolvers as an object
module.exports = { typeDefs, resolvers };